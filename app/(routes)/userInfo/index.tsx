import {View, Text} from 'react-native';
import React from 'react';
import UserInfoScreen from '@/screens/userInfo/UserInfo.screen';

const UserInfo = () => {
  return <UserInfoScreen />;
};

export default UserInfo;
